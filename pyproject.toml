[project]
name = "context-driven-prompt"
version = "0.1.0"
description = "An open-source, community-driven repository of structured and reusable prompts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
